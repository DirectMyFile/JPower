import jpower.gradle.JarAllTask

task jarAll(type: JarAllTask) {
    group = "build"
    description = "Builds a Jar with all JPower Modules"

    manifest {
        attributes "Implementation-Title": "JPower", "Implementation-Version": project.version
    }
}
