apply plugin: "build-dashboard"
apply plugin: "project-report"
apply plugin: "idea"
apply plugin: "eclipse"
apply plugin: "project-report"
apply plugin: 'build-announcements'

[dependencyReport, htmlDependencyReport].each { t ->
    t.projects = subprojects
}
