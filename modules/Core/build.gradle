processResources {
    expand([
            "jpowerVersion": project.version
    ])
}

jar {
    manifest {
        attributes("Premain-Class": "jpower.core.internal.PowerInternalSystem")
    }
}

test.include "jpower/core/test/**/*.java"
