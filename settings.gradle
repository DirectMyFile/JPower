include 'Core', 'Socket', 'Event', 'GUI', 'GCP', 'JSON', 'YAML', 'IRC', 'Script'

rootProject.children.each { prj ->
    prj.projectDir = new File("$rootDir/modules/$prj.name")
}

rootProject.name = "JPower"
